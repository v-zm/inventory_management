package com.vivek.inventorymanagement.features.inventory.enums

/**
 * [ProductViewTypeEnum] is type of views available to show items
 * */
enum class ProductViewTypeEnum(val type: Int) {
    LIST(0),
    GRID(1)
}